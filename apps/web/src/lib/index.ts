/**
 * Application library barrel — domain types and TheMealDB service layer.
 * UI agents should import from `$lib/api` and `$lib/types`, never call TheMealDB URLs directly.
 */
export * from './types';
export * from './api';
export * from './utils';
